# Проектная работа "Веб-ларек"

https://energizet.github.io/web-larek-frontend/

Стек: HTML, SCSS, TS, Webpack

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- src/pages/index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/index.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

Компоненты:

- src/components/api.ts - запросы к апи
    - Свойства
        - api - хранит базовый класс ApiUtil для работы с запросами
    - конструктор
        - принимает и заполняет api
    - Методы
        - loadProducts - делает запрос на /products и возвращает промис
        - order - принимает заказ, зелает запрос на /order, возвращает промис
- src/components/galleryView.ts - отображение карточек на главном экране
    - Свойства
        - gallery - элемент галереи
        - emitter - брокер событий
    - конструктор
        - находит элементы и заполняет поля
    - Методы
        - render - принимает список продуктов и заполняет gallery элементами CardCatalogView
- src/components/cardCatalogView.ts - карточка товара на главном экране, шаблон #card-catalog
    - Свойства
    - card - корневой элемент шаблона
    - category - элемент котегории товара
    - title - название товара
    - image - картинка товара
    - price - цена товара
    - emitter - брокер событий
        - конструктор
            - клонирует шаблон и заполняет поля
        - Методы
            - render - принимает товар, заполняет шаблон, вешает собитие открытия товара
            - onOpenCard - вызывает событие openCard
- src/components/modalView.ts - модальное окно
    - Свойства
        - modal - корневой элемент модального окна
        - container - отображаемая часть модалки
        - close - кнопка закрытия
        - content - поле с контентом модалки
    - конструктор
        - заполняет поля и вешает события управления модалкой
    - Методы
        - render - принимает элемент для отображения в content
        - show - открытие модалки
        - hide - закрытие модалки
- src/components/cardModalView.ts - карточка товара в модальном окне, шаблон #card-preview
    - Свойства
        - card - корневой элемент шаблона
        - image - картинка товара
        - category - элемент категории товара
        - title - название товара
        - description - описание товара
        - price - цена товара
        - button - кнопка добавления и открытия корзины
        - buttonStatus - состояние кнопки, либо добавить, либо открыть корзину
        - emitter - брокер событий
        - modal - компонент модального окна
        - cart - модель корзины
    - конструктор
        - клонирует шаблон и заполняет поля
    - Методы
        - render - принимает товар, заполняет шаблон, вешает обработчик кнопки, отправляет на отображение в модальном
          окне
        - onClick - проверяет статус кнопки, либо отправить событие addProduct и поменять статус, либо
          отправить openCart
- src/components/cartModalView.ts - корзина
    - Свойства
        - element - корневой элемент шаблона
        - list - элемент с товарами
        - button - кнопка подтверждения корзины
        - price - сумма в корзине
        - emitter - брокер событий
        - modal - компонент модального окна
        - cart - модель корзины
    - конструктор
        - клонирует шаблон и заполняет поля
    - Методы
        - render - принимает товар, заполняет шаблон, вешает обработчик кнопки, отправляет на отображение в модальном
          окне
        - onOpenOrder - отправляет событие openOrder
- src/components/cardCartView.ts - элемент корзины с товаром, шаблон #card-basket
    - Свойства
        - card - корневой элемент шаблона
        - index - элемент отображаюший индекс в корзине
        - title - описание товара
        - price - цена товара
        - button - кнопка удаления из корзины
        - emitter - брокер событий
        - cartModal - модель корзины
    - конструктор
        - клонирует шаблон и заполняет поля
    - Методы
        - render - принимает товар и индекс для отображения, заполняет шаблон, вешает событие на кнопку удаления товара
          из корзины
        - onDelete - обработчик кнопки удаления из корзины, принимает продукт который нужно удалить, вызывает событие
          deleteProduct и запускает render корзины
- src/components/orderModalView.ts - выбор оплаты и адреса, шаблон #order
    - Свойства
        - form - корневой элемент шаблона
        - buttonCard - кнопка оплаты онлайн
        - buttonCash - кнопка оплаты при получении
        - address - поле ввода адреса
        - button - кнопка продолжения
        - emitter - брокер событий
        - modal - компонент модального окна
        - order - модель заказа
    - конструктор
        - клонирует шаблон и заполняет поля
    - Методы
        - render - заполняет шаблон, вешает обработчики полей и кнопки, отправляет на отображение в модальном
          окне
        - updateButtonStatus - включает и выключает кнопку при заполнении поля
        - onInputAddress - сохраняет адрес в модель заказа
        - onClickCard - заполняет тип оплаты в заказе на онлайн
        - onClickCash - заполняет тип оплаты в заказе на при получении
        - onSubmit - обрабатывает отправку и вызывает событие openContacts
- src/components/contactsModalView.ts - ввод email и телефона, шаблон #contacts
    - Свойства
        - form - корневой элемент шаблона
        - email - элемент ввода почты
        - phone - ввода телефона
        - button - кнопка продолжения
        - emitter - брокер событий
        - modal - компонент модального окна
        - order - модель заказа
    - конструктор
        - клонирует шаблон и заполняет поля
    - Методы
        - render - заполняет шаблон, вешает обработчики полей и кнопки, отправляет на отображение в модальном
          окне
        - updateButtonStatus - включает и выключает кнопку при заполнении поля
        - onInputEmail - сохраняет почту в модель заказа
        - onInputPhone - сохраняет телефон в модель заказа
        - onSubmit - обрабатывает отправку и вызывает событие order
- src/components/successModalView.ts - окно подтверждения оплаты
    - Свойства
        - element - корневой элемент шаблона
        - description - описание сколько сумма заказа
        - button - кнопка закрытия модалки
        - modal - компонент модального окна
    - конструктор
        - клонирует шаблон и заполняет поля
    - Методы
        - render - принимает OrderResponse, заполняет шаблон, вешает обработчики полей и кнопки, отправляет на
          отображение в модальном окне
        - onClick - скрывает окно

Типы:

- src/types/cartModel.ts - модель корзины
    - Свойства
      - products - хранилище продуктов в корзине
    - Методы
      - add - добавление в корзину
      - delete - удаление из корзины
      - contains - проверка содержания в корзине
      - getProducts - получение товаров
      - getPrice - получение суммы корзины
      - clear - очищение корзины
- src/types/orderModel.ts - модель заказа
    - Свойства
      - payType - тип оплаты
      - address - адрес заказа
      - email - почта
      - phone - телефон
- src/types/models.ts - модели продуктов и запросов
  - ProductResponse - тип ответа на запрос товаров
    - Свойства
      - total - количество товаров
      - items - товары
  - Product - товар
    - Свойства
      - id - id товара
      - description - описание
      - image - картинка
      - title - название
      - category - категория
      - price - цена
  - Order - модель отправки заказа
    - Свойства
      - payment - тип оплаты
      - email - почта
      - phone - телефон
      - address - адрес
      - total - сумма оплаты
      - items - список товаров
  - OrderResponse - ответ оформления заказа
    - Свойства
      - id - заказа
      - total - сумма оплаты

События логические:

- addProduct - добавление товара в корзину
- deleteProduct - удаление товара из корзины
- order - выполнение заказа

События визуальные:

- onLoadedProducts - отображение загруженных товаров
- openCard - открытие карточки товара
- openCart - открытие корзины
- openOrder - открытие заказа
- openContacts - открытие контактов
- onOrdered - открытие подтверждения оплаты

## Описание

В `cartModel` и `orderModel` хранится состояние заказа

Каждый компонент в конструкторе создает элементы с которыми будет работать и метод render для заполнения элементов

Работа с модальным окном происходит в modalView

Компоненты могут заполнять состояние и вызывать события

Все события описаны в `index.ts` и там же происходит связывание отдельных частей приложения


1. Из каких основных частей состоит архитектура проекта?
    - брокер событий, класс Api, компоненты отображения
2. Зачем нужны эти части, какие функции они выполняют?
    - через брокер отправляются события, апи делает запросы на бек, компоненты выводят данные на экран
3. Как части взаимодействуют?
   - через события в брокере
4. Какие данные используются в приложении?
   - описал ранее
5. Из каких компонентов состоит приложение?
   - главный экран, товар на главном экране, модальное окно и компоненты которые отображаются в модальном окне, описано выше
6. Как реализованы процессы в приложении?
   - через события

## Установка и сборка

Для запуска проекта вам понадобится создать в корне файл с именем `.env` и разместить в нем следующее содержимое:

```
API_ORIGIN=https://larek-api.nomoreparties.co
```

```
npm install
npm run build:dev
```
